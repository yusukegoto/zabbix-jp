---
driver:
  name: vagrant

provisioner:
  name: chef_solo

platforms:
  # - name: ubuntu-12.04
  - name: centos-6.4

suites:
  - name: server
    run_list:
      - recipe[zabbix-jp::server]
    attributes:
